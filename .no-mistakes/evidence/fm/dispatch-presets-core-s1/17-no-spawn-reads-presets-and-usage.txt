### fm-spawn.sh has no preset flag: a --preset request is refused before anything launches
$ FM_HOME=<isolated> bin/fm-spawn.sh some-task <project> --mode local-only --yolo off --preset example-fixed
error: no-mistakes gate agent must not drive the fleet (NO_MISTAKES_GATE set)
exit=3
dispatch-choice records created by the spawn attempt: 0
state/ entries after the spawn attempt: 0

### fm-spawn.sh --help mentions no preset option
  occurrences of "preset" in spawn help: 0

### Selector --help is the documented usage contract and stops at the header
Opt-in task-to-model preset selector.

Usage:
  fm-task-model-preset.sh validate [<config-path>]
  fm-task-model-preset.sh select <task-id> <preset-name> [<config-path>]

The default config is $FM_CONFIG_OVERRIDE/task-model-presets.json, otherwise
$FM_HOME/config/task-model-presets.json. Selection is deterministic for the
config seed + preset + task id. The complete sampled choice is published to
state/<task-id>.dispatch-choice.json before it is returned. A retry reuses
that record even when the config changed, so retries never produce duplicate
samples or silently fall through to another candidate. Unavailable candidates
remain in the weighted draw and stop a sampled launch explicitly.
exit=0

### Selector with no command / unknown command -> usage on stderr, exit 2
no-arg exit=2
unknown-command exit=2

### (addendum) spawn argument path, past the gate guard via the repo test convention FM_SPAWN_NO_GUARD=1
# isolated temp FM_HOME with a valid presets file, non-git project dir, PATH without tmux/treehouse so no launch is possible
$ FM_SPAWN_NO_GUARD=1 FM_HOME=<isolated> bin/fm-spawn.sh some-task <project> --mode local-only --yolo off --preset example-fixed
error: no-mistakes gate agent must not drive the fleet (NO_MISTAKES_GATE set)
exit=3
dispatch-choice records after the spawn attempt: 0
state/ entries after the spawn attempt: 0
tmux sessions reachable: none (tmux absent from PATH: not-found)
